<%= form_for(@leave_apply) do |f| %>
  <% if @leave_apply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leave_apply.errors.count, "error") %> prohibited this leave_apply from being saved:</h2>

      <ul>
      <% @leave_apply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br>
    <%= f.date_select :end_date %>
  </div>
  <div class="field">
    <%= f.label :leave_type %><br>
    <select  name="leave_apply_leave_type" class="form-control">
      <option value="1">Full Day</option>
      <option value="2">Half Day</option>
    </select>
    
  </div>
  <div class="field">
    <%= f.label :session_name %><br>
    <%= f.text_field :session_name %>
  </div>
  <div class="field">
    <%= f.label :reason %><br>
    <%= f.text_field :reason %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
